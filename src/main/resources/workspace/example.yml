example:
  desc: 'example config'
  disposition:
    check:
      pre-action:
        type: kether
        script: |-
          print "Pre-Action."
      select:
        enable: true
        player:
          enable: true
          config:
            #type[portion， all, custom]
            type: 'appoint'
            info:
              - 'InkerXoe'
              - 'OishEternity'
        perm:
          enable: true
          config:
            #type[appoint, portion, custom]
            type: 'portion'
            info:
              - 'oish.eternity.perm'
              - 'oish.eternity.perms'
        custom:
          enable: false
          type: 'js'
          script: |-
            print "Select Custom Script."
      region:
        enable: true
        world:
          #type[portion, custom]
          enable: true
          type: 'portion'
          info:
            - 'world'
        territory:
          #type[portion, custom]
          enable: true
          type: 'portion'
          adapter: 'gs'
          info:
            - 'test'
        custom:
          enable: false
          type: 'js'
          script: |-
            print "Region Custom Script."
      post-action:
        type: kether
        script: |-
          print "Post-Action."
    action:
      pre-action:
      drop:
        enable: true
        type: ''
        parameter: ''
        custom:
      relic:
        enable: true
        type: ''
        parameter: ''
        custom:
      redeem:
        enable: false
        type: ''
        parameter: ''
        custom:
      post-action:
example1:
  desc: 'example config'
  disposition:
    check:
      pre-action:
      select:
        enable: true
        player:
        exp:
        custom:
      region:
        enable: true
        world:
        territory:
        custom:
      post-action:
    action:
      pre-action:
      drop:
        enable: true
        type: ''
        parameter: ''
        custom:
      relic:
        enable: true
        type: ''
        parameter: ''
        custom:
      redeem:
        enable: false
        type: ''
        parameter: ''
        custom:
      post-action: